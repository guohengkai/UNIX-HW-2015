1. Environment
    Ubuntu 13.04, gcc 4.7.3
2. Usage
    Use "./csv_convert_print" for details. And use "make test" for testing,
3. Algorithm
    Save csv file into binary file format. First write the identifier "BINARY", then write the number of rows and column. Finally write the table data one by one. For each data, first write the length of the string, then write the data, and at last write a parser with char of 255.
